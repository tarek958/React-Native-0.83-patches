# React-Native-0.83-patches
resolve several build failures and native integration issues that appeared after upgrading a project. The migration introduced a lot of breaking changes, especially around the Fabric renderer and the new architecture, so I had to patch a few popular libraries to make everything work smoothly again.
